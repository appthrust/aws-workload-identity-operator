---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: awsserviceaccountrolereplicasets.aws.identity.appthrust.io
spec:
  group: aws.identity.appthrust.io
  names:
    kind: AWSServiceAccountRoleReplicaSet
    listKind: AWSServiceAccountRoleReplicaSetList
    plural: awsserviceaccountrolereplicasets
    shortNames:
    - awsarrs
    singular: awsserviceaccountrolereplicaset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.desiredClusterCount
      name: Desired
      type: integer
    - jsonPath: .status.readyClusterCount
      name: ReadyClusters
      type: integer
    - jsonPath: .status.failureCount
      name: Failed
      type: integer
    - jsonPath: .status.conflictCount
      name: Conflicts
      priority: 1
      type: integer
    - jsonPath: .status.staleClusterCount
      name: Stale
      priority: 1
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AWSServiceAccountRoleReplicaSet fans out AWSServiceAccountRole
          children.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AWSServiceAccountRoleReplicaSetSpec defines fleet-scoped
              role fan-out.
            properties:
              placementRefs:
                description: |-
                  PlacementRefs references same-namespace OCM Placement objects. Multiple
                  refs are unioned by cluster identity.
                items:
                  description: PlacementRef identifies a same-namespace OCM Placement.
                  properties:
                    name:
                      description: Name is the name of the OCM Placement in the same
                        namespace.
                      maxLength: 253
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                    rolloutStrategy:
                      default:
                        type: All
                      description: |-
                        RolloutStrategy controls how generated AWSServiceAccountRole children
                        are applied across clusters selected by this placement ref.
                      properties:
                        all:
                          description: all defines required fields for RolloutStrategy
                            type All
                          properties:
                            maxFailures:
                              anyOf:
                              - type: integer
                              - type: string
                              default: 0
                              description: |-
                                MaxFailures is a percentage or number of clusters in the current rollout that can fail before
                                proceeding to the next rollout. Fail means the cluster has a failed status or timeout status
                                (does not reach successful status after ProgressDeadline).
                                Once the MaxFailures is breached, the rollout will stop.
                                MaxFailures is only considered for rollout types Progressive and ProgressivePerGroup. For
                                Progressive, this is considered over the total number of clusters. For ProgressivePerGroup,
                                this is considered according to the size of the current group. For both Progressive and
                                ProgressivePerGroup, the MaxFailures does not apply for MandatoryDecisionGroups, which tolerate
                                no failures.
                                Default is that no failures are tolerated.
                              pattern: ^((100|[0-9]{1,2})%|[0-9]+)$
                              x-kubernetes-int-or-string: true
                            minSuccessTime:
                              default: "0"
                              description: |-
                                MinSuccessTime is a "soak" time. In other words, the minimum amount of time the workload
                                applier controller will wait from the start of each rollout before proceeding (assuming a
                                successful state has been reached and MaxFailures wasn't breached).
                                MinSuccessTime is only considered for rollout types Progressive and ProgressivePerGroup.
                                The default value is 0 meaning the workload applier proceeds immediately after a successful
                                state is reached.
                                MinSuccessTime must be defined in [0-9h]|[0-9m]|[0-9s] format examples; 2h , 90m , 360s
                              type: string
                            progressDeadline:
                              default: None
                              description: |-
                                ProgressDeadline defines how long workload applier controller will wait for the workload to
                                reach a successful state in the cluster.
                                If the workload does not reach a successful state after ProgressDeadline, will stop waiting
                                and workload will be treated as "timeout" and be counted into MaxFailures. Once the MaxFailures
                                is breached, the rollout will stop.
                                ProgressDeadline default value is "None", meaning the workload applier will wait for a
                                successful state indefinitely.
                                ProgressDeadline must be defined in [0-9h]|[0-9m]|[0-9s] format examples; 2h , 90m , 360s
                              pattern: ^(([0-9])+[h|m|s])|None$
                              type: string
                          type: object
                        progressive:
                          description: progressive defines required fields for RolloutStrategy
                            type Progressive
                          properties:
                            mandatoryDecisionGroups:
                              description: |-
                                List of the decision groups names or indexes to apply the workload first and fail if workload
                                did not reach successful state.
                                GroupName or GroupIndex must match with the decisionGroups defined in the placement's
                                decisionStrategy
                              items:
                                description: |-
                                  MandatoryDecisionGroup set the decision group name or group index.
                                  GroupName is considered first to select the decisionGroups then GroupIndex.
                                properties:
                                  groupIndex:
                                    description: |-
                                      groupIndex of the decision group should match the placementDecisions label value with label key
                                      cluster.open-cluster-management.io/decision-group-index
                                    format: int32
                                    type: integer
                                  groupName:
                                    description: |-
                                      groupName of the decision group should match the placementDecisions label value with label key
                                      cluster.open-cluster-management.io/decision-group-name
                                    type: string
                                type: object
                              type: array
                            maxConcurrency:
                              anyOf:
                              - type: integer
                              - type: string
                              description: |-
                                maxConcurrency is the max number of clusters to deploy workload concurrently. The default value
                                for MaxConcurrency is determined from the clustersPerDecisionGroup defined in the
                                placement->DecisionStrategy.
                              pattern: ^((100|[0-9]{1,2})%|[0-9]+)$
                              x-kubernetes-int-or-string: true
                            maxFailures:
                              anyOf:
                              - type: integer
                              - type: string
                              default: 0
                              description: |-
                                MaxFailures is a percentage or number of clusters in the current rollout that can fail before
                                proceeding to the next rollout. Fail means the cluster has a failed status or timeout status
                                (does not reach successful status after ProgressDeadline).
                                Once the MaxFailures is breached, the rollout will stop.
                                MaxFailures is only considered for rollout types Progressive and ProgressivePerGroup. For
                                Progressive, this is considered over the total number of clusters. For ProgressivePerGroup,
                                this is considered according to the size of the current group. For both Progressive and
                                ProgressivePerGroup, the MaxFailures does not apply for MandatoryDecisionGroups, which tolerate
                                no failures.
                                Default is that no failures are tolerated.
                              pattern: ^((100|[0-9]{1,2})%|[0-9]+)$
                              x-kubernetes-int-or-string: true
                            minSuccessTime:
                              default: "0"
                              description: |-
                                MinSuccessTime is a "soak" time. In other words, the minimum amount of time the workload
                                applier controller will wait from the start of each rollout before proceeding (assuming a
                                successful state has been reached and MaxFailures wasn't breached).
                                MinSuccessTime is only considered for rollout types Progressive and ProgressivePerGroup.
                                The default value is 0 meaning the workload applier proceeds immediately after a successful
                                state is reached.
                                MinSuccessTime must be defined in [0-9h]|[0-9m]|[0-9s] format examples; 2h , 90m , 360s
                              type: string
                            progressDeadline:
                              default: None
                              description: |-
                                ProgressDeadline defines how long workload applier controller will wait for the workload to
                                reach a successful state in the cluster.
                                If the workload does not reach a successful state after ProgressDeadline, will stop waiting
                                and workload will be treated as "timeout" and be counted into MaxFailures. Once the MaxFailures
                                is breached, the rollout will stop.
                                ProgressDeadline default value is "None", meaning the workload applier will wait for a
                                successful state indefinitely.
                                ProgressDeadline must be defined in [0-9h]|[0-9m]|[0-9s] format examples; 2h , 90m , 360s
                              pattern: ^(([0-9])+[h|m|s])|None$
                              type: string
                          type: object
                        progressivePerGroup:
                          description: progressivePerGroup defines required fields
                            for RolloutStrategy type ProgressivePerGroup
                          properties:
                            mandatoryDecisionGroups:
                              description: |-
                                List of the decision groups names or indexes to apply the workload first and fail if workload
                                did not reach successful state.
                                GroupName or GroupIndex must match with the decisionGroups defined in the placement's
                                decisionStrategy
                              items:
                                description: |-
                                  MandatoryDecisionGroup set the decision group name or group index.
                                  GroupName is considered first to select the decisionGroups then GroupIndex.
                                properties:
                                  groupIndex:
                                    description: |-
                                      groupIndex of the decision group should match the placementDecisions label value with label key
                                      cluster.open-cluster-management.io/decision-group-index
                                    format: int32
                                    type: integer
                                  groupName:
                                    description: |-
                                      groupName of the decision group should match the placementDecisions label value with label key
                                      cluster.open-cluster-management.io/decision-group-name
                                    type: string
                                type: object
                              type: array
                            maxFailures:
                              anyOf:
                              - type: integer
                              - type: string
                              default: 0
                              description: |-
                                MaxFailures is a percentage or number of clusters in the current rollout that can fail before
                                proceeding to the next rollout. Fail means the cluster has a failed status or timeout status
                                (does not reach successful status after ProgressDeadline).
                                Once the MaxFailures is breached, the rollout will stop.
                                MaxFailures is only considered for rollout types Progressive and ProgressivePerGroup. For
                                Progressive, this is considered over the total number of clusters. For ProgressivePerGroup,
                                this is considered according to the size of the current group. For both Progressive and
                                ProgressivePerGroup, the MaxFailures does not apply for MandatoryDecisionGroups, which tolerate
                                no failures.
                                Default is that no failures are tolerated.
                              pattern: ^((100|[0-9]{1,2})%|[0-9]+)$
                              x-kubernetes-int-or-string: true
                            minSuccessTime:
                              default: "0"
                              description: |-
                                MinSuccessTime is a "soak" time. In other words, the minimum amount of time the workload
                                applier controller will wait from the start of each rollout before proceeding (assuming a
                                successful state has been reached and MaxFailures wasn't breached).
                                MinSuccessTime is only considered for rollout types Progressive and ProgressivePerGroup.
                                The default value is 0 meaning the workload applier proceeds immediately after a successful
                                state is reached.
                                MinSuccessTime must be defined in [0-9h]|[0-9m]|[0-9s] format examples; 2h , 90m , 360s
                              type: string
                            progressDeadline:
                              default: None
                              description: |-
                                ProgressDeadline defines how long workload applier controller will wait for the workload to
                                reach a successful state in the cluster.
                                If the workload does not reach a successful state after ProgressDeadline, will stop waiting
                                and workload will be treated as "timeout" and be counted into MaxFailures. Once the MaxFailures
                                is breached, the rollout will stop.
                                ProgressDeadline default value is "None", meaning the workload applier will wait for a
                                successful state indefinitely.
                                ProgressDeadline must be defined in [0-9h]|[0-9m]|[0-9s] format examples; 2h , 90m , 360s
                              pattern: ^(([0-9])+[h|m|s])|None$
                              type: string
                          type: object
                        type:
                          default: All
                          enum:
                          - All
                          - Progressive
                          - ProgressivePerGroup
                          type: string
                      type: object
                  required:
                  - name
                  type: object
                maxItems: 16
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              template:
                description: Template describes the generated AWSServiceAccountRole
                  children.
                properties:
                  metadata:
                    description: |-
                      Metadata is copied to generated children after reserved metadata is
                      rejected by admission.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        type: object
                    type: object
                  spec:
                    description: Spec is copied to each generated AWSServiceAccountRole
                      child.
                    properties:
                      policyARNs:
                        items:
                          maxLength: 2048
                          minLength: 1
                          pattern: ^arn:aws[a-z0-9-]*:iam::(aws|[0-9]{12}):policy/[\w+=,.@/-]+$
                          type: string
                        maxItems: 10
                        type: array
                        x-kubernetes-list-type: set
                      policyDocument:
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      serviceAccount:
                        description: ServiceAccountSubject identifies a Kubernetes
                          ServiceAccount.
                        properties:
                          name:
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                            type: string
                          namespace:
                            maxLength: 63
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                        x-kubernetes-validations:
                        - message: spec.serviceAccount is immutable
                          rule: self == oldSelf
                    required:
                    - serviceAccount
                    type: object
                    x-kubernetes-validations:
                    - message: at least one of spec.policyARNs or spec.policyDocument
                        is required
                      rule: (has(self.policyARNs) && self.policyARNs.size() > 0) ||
                        has(self.policyDocument)
                required:
                - spec
                type: object
            required:
            - placementRefs
            - template
            type: object
            x-kubernetes-validations:
            - message: spec.template.spec.serviceAccount is immutable
              rule: self.template.spec.serviceAccount == oldSelf.template.spec.serviceAccount
          status:
            description: AWSServiceAccountRoleReplicaSetStatus reports fleet fan-out
              state.
            properties:
              appliedClusterCount:
                format: int32
                type: integer
              clusters:
                items:
                  description: AWSServiceAccountRoleClusterSummary records the child
                    role state per cluster.
                  properties:
                    clusterName:
                      minLength: 1
                      type: string
                    message:
                      maxLength: 32768
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    phase:
                      description: AWSServiceAccountRoleClusterPhase describes per-cluster
                        fan-out state.
                      enum:
                      - Pending
                      - Ready
                      - Conflict
                      - Failed
                      - TimedOut
                      type: string
                    reason:
                      maxLength: 1024
                      type: string
                  required:
                  - clusterName
                  - phase
                  type: object
                maxItems: 100
                type: array
                x-kubernetes-list-map-keys:
                - clusterName
                x-kubernetes-list-type: map
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              conflictCount:
                format: int32
                type: integer
              desiredClusterCount:
                format: int32
                type: integer
              failedClusters:
                items:
                  description: AWSServiceAccountRoleClusterFailure records a failed
                    cluster fan-out path.
                  properties:
                    clusterName:
                      minLength: 1
                      type: string
                    message:
                      maxLength: 32768
                      type: string
                    phase:
                      description: AWSServiceAccountRoleClusterPhase describes per-cluster
                        fan-out state.
                      enum:
                      - Pending
                      - Ready
                      - Conflict
                      - Failed
                      - TimedOut
                      type: string
                    reason:
                      maxLength: 1024
                      type: string
                  required:
                  - clusterName
                  - phase
                  type: object
                maxItems: 50
                type: array
                x-kubernetes-list-map-keys:
                - clusterName
                x-kubernetes-list-type: map
              failureCount:
                format: int32
                type: integer
              observedGeneration:
                format: int64
                minimum: 0
                type: integer
              placements:
                items:
                  description: |-
                    AWSServiceAccountRolePlacementStatus reports one placement ref resolution
                    and rollout state.
                  properties:
                    availableDecisionGroups:
                      type: string
                    conditions:
                      items:
                        description: Condition contains details for one aspect of
                          the current state of this API Resource.
                        properties:
                          lastTransitionTime:
                            description: |-
                              lastTransitionTime is the last time the condition transitioned from one status to another.
                              This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                            format: date-time
                            type: string
                          message:
                            description: |-
                              message is a human readable message indicating details about the transition.
                              This may be an empty string.
                            maxLength: 32768
                            type: string
                          observedGeneration:
                            description: |-
                              observedGeneration represents the .metadata.generation that the condition was set based upon.
                              For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                              with respect to the current state of the instance.
                            format: int64
                            minimum: 0
                            type: integer
                          reason:
                            description: |-
                              reason contains a programmatic identifier indicating the reason for the condition's last transition.
                              Producers of specific condition types may define expected values and meanings for this field,
                              and whether the values are considered a guaranteed API.
                              The value should be a CamelCase string.
                              This field may not be empty.
                            maxLength: 1024
                            minLength: 1
                            pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                            type: string
                          status:
                            description: status of the condition, one of True, False,
                              Unknown.
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            description: type of condition in CamelCase or in foo.example.com/CamelCase.
                            maxLength: 316
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                            type: string
                        required:
                        - lastTransitionTime
                        - message
                        - reason
                        - status
                        - type
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - type
                      x-kubernetes-list-type: map
                    name:
                      minLength: 1
                      type: string
                    rollout:
                      description: AWSServiceAccountRoleRolloutSummary reports OCM
                        rollout progress counts.
                      properties:
                        conditions:
                          items:
                            description: Condition contains details for one aspect
                              of the current state of this API Resource.
                            properties:
                              lastTransitionTime:
                                description: |-
                                  lastTransitionTime is the last time the condition transitioned from one status to another.
                                  This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                                format: date-time
                                type: string
                              message:
                                description: |-
                                  message is a human readable message indicating details about the transition.
                                  This may be an empty string.
                                maxLength: 32768
                                type: string
                              observedGeneration:
                                description: |-
                                  observedGeneration represents the .metadata.generation that the condition was set based upon.
                                  For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                                  with respect to the current state of the instance.
                                format: int64
                                minimum: 0
                                type: integer
                              reason:
                                description: |-
                                  reason contains a programmatic identifier indicating the reason for the condition's last transition.
                                  Producers of specific condition types may define expected values and meanings for this field,
                                  and whether the values are considered a guaranteed API.
                                  The value should be a CamelCase string.
                                  This field may not be empty.
                                maxLength: 1024
                                minLength: 1
                                pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                                type: string
                              status:
                                description: status of the condition, one of True,
                                  False, Unknown.
                                enum:
                                - "True"
                                - "False"
                                - Unknown
                                type: string
                              type:
                                description: type of condition in CamelCase or in
                                  foo.example.com/CamelCase.
                                maxLength: 316
                                pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                                type: string
                            required:
                            - lastTransitionTime
                            - message
                            - reason
                            - status
                            - type
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                          - type
                          x-kubernetes-list-type: map
                        failed:
                          format: int32
                          minimum: 0
                          type: integer
                        succeeded:
                          format: int32
                          minimum: 0
                          type: integer
                        timedOut:
                          format: int32
                          minimum: 0
                          type: integer
                        total:
                          format: int32
                          minimum: 0
                          type: integer
                        updating:
                          format: int32
                          minimum: 0
                          type: integer
                      type: object
                    selectedClusterCount:
                      format: int32
                      minimum: 0
                      type: integer
                  required:
                  - name
                  type: object
                maxItems: 16
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              readyClusterCount:
                format: int32
                type: integer
              rollout:
                description: AWSServiceAccountRoleRolloutSummary reports OCM rollout
                  progress counts.
                properties:
                  conditions:
                    items:
                      description: Condition contains details for one aspect of the
                        current state of this API Resource.
                      properties:
                        lastTransitionTime:
                          description: |-
                            lastTransitionTime is the last time the condition transitioned from one status to another.
                            This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                          format: date-time
                          type: string
                        message:
                          description: |-
                            message is a human readable message indicating details about the transition.
                            This may be an empty string.
                          maxLength: 32768
                          type: string
                        observedGeneration:
                          description: |-
                            observedGeneration represents the .metadata.generation that the condition was set based upon.
                            For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                            with respect to the current state of the instance.
                          format: int64
                          minimum: 0
                          type: integer
                        reason:
                          description: |-
                            reason contains a programmatic identifier indicating the reason for the condition's last transition.
                            Producers of specific condition types may define expected values and meanings for this field,
                            and whether the values are considered a guaranteed API.
                            The value should be a CamelCase string.
                            This field may not be empty.
                          maxLength: 1024
                          minLength: 1
                          pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                          type: string
                        status:
                          description: status of the condition, one of True, False,
                            Unknown.
                          enum:
                          - "True"
                          - "False"
                          - Unknown
                          type: string
                        type:
                          description: type of condition in CamelCase or in foo.example.com/CamelCase.
                          maxLength: 316
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                          type: string
                      required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - type
                    x-kubernetes-list-type: map
                  failed:
                    format: int32
                    minimum: 0
                    type: integer
                  succeeded:
                    format: int32
                    minimum: 0
                    type: integer
                  timedOut:
                    format: int32
                    minimum: 0
                    type: integer
                  total:
                    format: int32
                    minimum: 0
                    type: integer
                  updating:
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              selectedClusterCount:
                format: int32
                type: integer
              staleClusterCount:
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
