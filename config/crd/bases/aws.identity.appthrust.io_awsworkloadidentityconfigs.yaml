---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: awsworkloadidentityconfigs.aws.identity.appthrust.io
spec:
  group: aws.identity.appthrust.io
  names:
    kind: AWSWorkloadIdentityConfig
    listKind: AWSWorkloadIdentityConfigList
    plural: awsworkloadidentityconfigs
    shortNames:
    - awic
    singular: awsworkloadidentityconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AWSWorkloadIdentityConfig configures namespace-scoped workload
          identity delivery.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AWSWorkloadIdentityConfigSpec defines namespace-scoped workload
              identity delivery.
            properties:
              region:
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: spec.region is immutable
                  rule: self == oldSelf
              type:
                description: DeliveryType selects the workload identity delivery strategy.
                enum:
                - SelfHostedIRSA
                - EKSPodIdentity
                type: string
                x-kubernetes-validations:
                - message: spec.type is immutable
                  rule: self == oldSelf
            required:
            - region
            - type
            type: object
          status:
            description: AWSWorkloadIdentityConfigStatus reports delivery and AWS
              resource state.
            properties:
              ackResources:
                items:
                  description: ACKResourceStatus reports the status copied from an
                    operator-owned ACK child.
                  properties:
                    apiVersion:
                      type: string
                    conditions:
                      items:
                        description: ACKResourceCondition mirrors the public shape
                          of ACK status conditions.
                        properties:
                          lastTransitionTime:
                            format: date-time
                            type: string
                          message:
                            type: string
                          reason:
                            type: string
                          status:
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            type: string
                        required:
                        - status
                        - type
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - type
                      x-kubernetes-list-type: map
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  - namespace
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - apiVersion
                - kind
                - namespace
                - name
                x-kubernetes-list-type: map
              bucketName:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              issuerHostPath:
                type: string
              observedGeneration:
                format: int64
                type: integer
              oidcProviderARN:
                type: string
              publishedKeyID:
                description: |-
                  PublishedKeyID is the signing key ID published to the self-hosted issuer
                  objects in S3. When this matches the current signing Secret key ID, the
                  controller skips re-uploading discovery and JWKS objects.
                type: string
              resolvedClusterName:
                description: |-
                  ResolvedClusterName records the multicluster-runtime cluster identifier
                  used by the latest ready self-hosted inventory resolution.
                type: string
              webhookRuntimeCertNotAfter:
                format: date-time
                type: string
              webhookRuntimeNamespace:
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: metadata.name must be default
          rule: self.metadata.name == 'default'
    served: true
    storage: true
    subresources:
      status: {}
