---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: awsserviceaccountroles.aws.identity.appthrust.io
spec:
  group: aws.identity.appthrust.io
  names:
    kind: AWSServiceAccountRole
    listKind: AWSServiceAccountRoleList
    plural: awsserviceaccountroles
    shortNames:
    - awsar
    singular: awsserviceaccountrole
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.serviceAccount.name
      name: ServiceAccount
      type: string
    - jsonPath: .spec.serviceAccount.namespace
      name: SA-Namespace
      priority: 1
      type: string
    - jsonPath: .status.deliveryType
      name: Delivery
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      priority: 1
      type: string
    - jsonPath: .status.roleARN
      name: RoleARN
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AWSServiceAccountRole requests IAM permissions for one Kubernetes
          ServiceAccount.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AWSServiceAccountRoleSpec defines the desired IAM role and
              policy delivery.
            properties:
              policyARNs:
                items:
                  maxLength: 2048
                  minLength: 1
                  pattern: ^arn:aws[a-z0-9-]*:iam::(aws|[0-9]{12}):policy/[\w+=,.@/-]+$
                  type: string
                maxItems: 10
                type: array
                x-kubernetes-list-type: set
              policyDocument:
                description: |-
                  PolicyDocument is an inline customer-managed IAM policy document attached
                  to the generated role. The top-level shape is bounded so the stored object
                  stays small and constant time to read; IAM polymorphism (Action/Resource/
                  Principal/Condition) is preserved inside each Statement.
                properties:
                  Id:
                    description: ID is an optional identifier for the policy document.
                    maxLength: 128
                    minLength: 1
                    type: string
                  Statement:
                    description: |-
                      Statement is the bounded list of IAM policy statements. Each statement
                      preserves IAM polymorphic keys (Action/Resource/Principal/Condition) but
                      is capped on top-level key count so a single statement cannot grow
                      unboundedly.
                    items:
                      maxProperties: 16
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    maxItems: 50
                    minItems: 1
                    type: array
                  Version:
                    description: |-
                      Version is the IAM policy language version. Only the two AWS-recognized
                      values are accepted.
                    enum:
                    - "2008-10-17"
                    - "2012-10-17"
                    type: string
                required:
                - Statement
                type: object
              serviceAccount:
                description: ServiceAccountSubject identifies a Kubernetes ServiceAccount.
                properties:
                  name:
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                    type: string
                  namespace:
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - name
                - namespace
                type: object
                x-kubernetes-validations:
                - message: spec.serviceAccount is immutable
                  rule: self == oldSelf
            required:
            - serviceAccount
            type: object
            x-kubernetes-validations:
            - message: exactly one of spec.policyARNs or spec.policyDocument must
                be specified
              rule: (has(self.policyARNs) && self.policyARNs.size() > 0) != has(self.policyDocument)
          status:
            description: AWSServiceAccountRoleStatus reports resolved IAM and delivery
              state.
            properties:
              ackResources:
                items:
                  description: ACKResourceStatus reports the status copied from an
                    operator-owned ACK child.
                  properties:
                    apiVersion:
                      maxLength: 316
                      minLength: 1
                      type: string
                    conditions:
                      items:
                        description: ACKResourceCondition mirrors the public shape
                          of ACK status conditions.
                        properties:
                          lastTransitionTime:
                            format: date-time
                            type: string
                          message:
                            maxLength: 32768
                            type: string
                          reason:
                            maxLength: 1024
                            type: string
                          status:
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            maxLength: 316
                            minLength: 1
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                            type: string
                        required:
                        - status
                        - type
                        type: object
                      maxItems: 32
                      type: array
                      x-kubernetes-list-map-keys:
                      - type
                      x-kubernetes-list-type: map
                    kind:
                      maxLength: 63
                      minLength: 1
                      type: string
                    name:
                      maxLength: 253
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                    namespace:
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  - namespace
                  type: object
                maxItems: 16
                type: array
                x-kubernetes-list-map-keys:
                - apiVersion
                - kind
                - namespace
                - name
                x-kubernetes-list-type: map
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 32
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deliveryType:
                description: |-
                  DeliveryType records the last resolved delivery strategy used by the
                  role. It is used during deletion if the namespace config was force-deleted.
                enum:
                - SelfHostedIRSA
                - EKSPodIdentity
                - EKSIRSA
                type: string
              generatedPolicyARN:
                description: |-
                  GeneratedPolicyARN is the customer-managed IAM Policy ARN generated
                  when `spec.policyDocument` is set. The shape mirrors the items pattern
                  on `spec.policyARNs`.
                maxLength: 2048
                pattern: ^arn:aws[a-z0-9-]*:iam::(aws|[0-9]{12}):policy/[\w+=,.@/-]+$
                type: string
              observedGeneration:
                format: int64
                minimum: 0
                type: integer
              resolvedClusterName:
                description: |-
                  ResolvedClusterName records the last ready multicluster-runtime cluster
                  identifier for SelfHostedIRSA delivery cleanup. The controller encodes
                  the identifier as `<namespace>/<name>` via
                  `types.NamespacedName.String()`, where both segments are DNS-1123
                  subdomains (and are currently equal — see `logicalClusterName` in
                  `internal/inventory/resolver.go`). The bound covers two maximum-length
                  subdomain segments and the separating slash (253 + 1 + 253).
                maxLength: 507
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                type: string
              roleARN:
                description: RoleARN is the IAM Role ARN generated for the bound ServiceAccount.
                maxLength: 2048
                pattern: ^arn:aws[a-z0-9-]*:iam::[0-9]{12}:role/[\w+=,.@/-]+$
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
